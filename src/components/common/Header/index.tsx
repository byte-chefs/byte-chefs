import { FC } from 'react'

const Header: FC = () => {
  return (
    <header className="inset-0 flex h-20 w-full items-center justify-center drop-shadow-md md:fixed">
      Header
    </header>
  )
}

export default Header
